# 监督者守则

这份文件约束的是我（用户），不是 LLM。每次派活前重读一遍。
(对AGENT的提示:以下只针对写代码等长程需求，没必要apt install都要求这样)

## 派活前

1. 任务必须先有书面计划：做什么、**明确不做什么**、怎么验收。我批的是计划，不是结果。
2. 一句话丢进去 = 违规。说不清验收标准的任务，先自己想清楚，再派。

## 干活中

3. 逐 commit 看 diff，不攒批追认。
4. 先查越界：diff 超出声明的计划，整批打回，不管代码多漂亮。

## 收货前

5. 测试当代码审：把被测逻辑删掉，测试还过 → 假测试，打回。
6. 文档里的每条命令、每个数字（体积/计数/耗时），亲自跑过才签收。
7. 读不懂的代码不许合并。要么让 agent 讲到我懂，要么重写到我能懂。

## 定期

8. 删除评审：主动问"该删什么"。LLM 永远不会替我做减法。
9. 尺子检查：这个项目在回答什么题？我有没有拿错的尺子在量它？

## 一票否决

- 选"保证能答对但答案无意义"的题之前，必须先说服自己：答案会改变谁的决策。
